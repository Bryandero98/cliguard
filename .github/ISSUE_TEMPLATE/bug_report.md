---
name: Bug report
about: Something cliguard did (or didn't do) that looks wrong
title: ""
labels: bug
---

**What happened**

**What you expected instead**

**Minimal reproduction**

The exact command you ran, plus the smallest CLI entry file (or a link to a repo) that reproduces it. If it's a diff-classification issue, include the two contracts (or the two versions of the entry file) being compared.

```sh
npx cliguard check ./bin/cli.js --adapter <commander|cac|yargs>
```

**Environment**

- cliguard version:
- Node version:
- CLI framework + version (commander / cac / yargs):
- OS:
